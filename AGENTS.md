# AGENTS.md

The instructions for this repository live in **[CLAUDE.md](CLAUDE.md)** — read
that file fully before touching anything. It is the single source of truth for
repo structure, git workflow, Go style, architectural invariants, the startup
sequence, and the current milestone.

This file exists only so agents that look for `AGENTS.md` find their way there.
Do not copy the rules back into this file: two copies drift, and the previous
duplicate had gone badly stale — it still described the M1-era design long after
the codebase had moved on.
