version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - prealloc
    - unconvert
    - unparam

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-same-issues: 0
