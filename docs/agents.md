# Agent Notes

Use Pi Coding as the platform. Build project behavior as Pi extensions, skills, prompt templates, themes, or web UI modules.

The old Go-native harness is archived under `archived/go-harness/` and is not the active architecture.

Before changing behavior, read `docs/architecture.md` and `docs/roadmap.md`.
